import { test } from '@playwright/test';

// Live suite is parked. The block-mode-era assertions reference page-object
// methods removed in PR #70. Will be rewritten alongside the live-suite
// rewrite (out of scope for the current mock-suite rewrite).
test.fixme('rewrite pending — out of scope for the mock-suite rewrite', () => {});
