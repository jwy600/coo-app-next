import { test } from '@playwright/test';

// Stubbed in Phase 1 of the e2e rewrite. Retained as a placeholder so its
// future coverage isn't forgotten; rewrite scheduled for Phase 7.
test.fixme('rewrite pending — see tasks/plan.md Phase 7', () => {});
