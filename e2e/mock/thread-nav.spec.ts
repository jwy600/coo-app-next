import { test } from '@playwright/test';

// Stubbed in Phase 1 of the e2e rewrite — original block-mode coverage was
// removed when api-mocks.ts and the page-object cluster were retired. The
// thread CRUD smoke tests will be rewritten in Phase 7.
test.fixme('rewrite pending — see tasks/plan.md Phase 7', () => {});
